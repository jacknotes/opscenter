apiVersion: v1
kind: Service
metadata:
  name: opscenter
  namespace: ops
  labels:
    app: opscenter
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 18080
    protocol: TCP
    name: http
  selector:
    app: opscenter
